# Historical release-note fixture

This fixture models notes emitted from imported history. The local Content
issues `#308` and `#287` are unavailable in the current repository, while the
replacement capability boundary remains tracked by
`atrinik/atrinik#266` and
https://github.com/atrinik/atrinik/issues/266.

Fix #308 - preserve the historical attribution.
Fixes #287.
