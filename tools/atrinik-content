#!/usr/bin/env python3
"""Repository entry point for the versioned lossless content CLI."""

from pathlib import Path
import sys


sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from tools.content_core.cli import main


if __name__ == "__main__":
    raise SystemExit(main())
